import classes

def initiate(population_size: int)-> Population:
    pass

def modify()-> list:
    pass

def stop(P,t):

def evolutionary_algorithm():
    t: int = 0
    P = []
    P[t] = initiate(population_size)
    P[t].rate
    while(not stop(P,t)):
        t=t+1
        modify(P[t])
        P[t].rate
    return(P[t])